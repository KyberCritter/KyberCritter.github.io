---
description: Build the Jekyll site into _site/
---

# Build the site

Build the site into `_site/` exactly as CI does:

```bash
bundle exec jekyll build
```

If the build fails, fix the reported errors and rebuild until it succeeds.
Report the outcome to the user.
