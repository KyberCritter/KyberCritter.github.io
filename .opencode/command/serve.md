---
description: Serve the Jekyll site locally with live reload
---

# Serve locally

Start the local Jekyll development server with live reload on port 4000:

```bash
bundle exec jekyll serve --livereload --port 4000
```

The site is available at `http://localhost:4000`. Keep the server running so
the user can preview, and report the URL back to them.
